export type UserType = {
    id: number,
    username: string,
    realname: string,
    authority: number,
    token: string
 }

export type ModpackType = {
    id: number,
    name: string,
    link: string,
    description: string,
    image: string,
    score: number
}